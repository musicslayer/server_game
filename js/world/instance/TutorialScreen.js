const DynamicScreen = require("./DynamicScreen.js");

// TODO Make this instance (not dynamic) and then load multiple of these to make a full tutorial.
class TutorialScreen extends DynamicScreen {
}

module.exports = TutorialScreen;