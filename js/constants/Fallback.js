class Fallback {
    // These variables must point to a valid location on every currently existing world across all servers.
    static FALLBACK_MAP_NAME = "city";
    static FALLBACK_SCREEN_NAME = "field1";
    static FALLBACK_X = 0;
    static FALLBACK_Y = 0;
}

module.exports = Fallback;