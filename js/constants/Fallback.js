class Fallback {
    // These variables must point to a valid location on every currently existing world across all servers.
    static FALLBACK_LOCATION_MAP_NAME = "city";
    static FALLBACK_LOCATION_SCREEN_NAME = "field1";
    static FALLBACK_LOCATION_X = 0;
    static FALLBACK_LOCATION_Y = 0;
}

module.exports = Fallback;